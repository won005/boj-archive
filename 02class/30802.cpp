#include <iostream>
using namespace std;

int main(){
    int all,packP, packT;
    int per[6];
    cin >> all;
    for(int i : per){
        cin >> per[i];
    }
    cin >>packT >>packP;

//문제 이해 덜됨
}